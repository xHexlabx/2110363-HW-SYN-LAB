# 2110363-HW-SYN-LAB

-----

## 📚 Tutorials & Examples

ส่วนนี้รวบรวมตัวอย่าง (Examples) ที่เรียบเรียงจากเนื้อหาพื้นฐานไปจนถึงระดับกลาง เพื่อใช้สำหรับการทบทวน Verilog Syntax และฝึกฝนการเขียน Testbench ด้วย Cocotb เนื้อหาอ้างอิงจากเอกสาร `HWSynBrief.pdf`

### 📂 Directory Structure

แต่ละ Example จะถูกจัดเก็บในโครงสร้างดังนี้:

  * **`src/`**: เก็บ Source code ภาษา Verilog (`.v`)
  * **`sim/`**: เก็บ Verilog Testbench แบบดั้งเดิม สำหรับรันด้วย Icarus Verilog
  * **`cocotb/`**: เก็บ Python Testbench และ Makefile สำหรับรัน Simulation ด้วย Cocotb

-----

### 🚀 List of Examples

#### **1. Basic Gate & Structure (`example_1`)**

เรียนรู้โครงสร้างพื้นฐานของ Module, การประกาศ Input/Output และการใช้ **Continuous Assignment** (`assign`) สำหรับวงจร Logic Gate พื้นฐาน

  * **Key Concept:** `assign out = a & b;`

#### **2. Arithmetic Operations (`example_2`)**

การใช้อักขระทางคณิตศาสตร์ (Arithmetic Operators) เพื่อสร้างวงจร Full Adder และการใช้ **Concatenation** (`{}`) เพื่อรวมสัญญาณ

  * **Key Concept:** `assign {cout, sum} = a + b + cin;`

#### **3. Vectors & Ranges (`example_3`)**

การประกาศขนาดของสัญญาณ (Bus Width) เช่น `[3:0]` สำหรับการจัดการข้อมูลแบบกลุ่มบิต ในวงจร 4-bit Half Adder

  * **Key Concept:** `input [3:0] a;`

#### **4. Combinational Logic - Procedural (`example_4`)**

การใช้วงจร Combinational ที่ซับซ้อนขึ้นด้วย **Procedural Assignment** ภายใน `always @(*)` block และการใช้ `case` statement สำหรับสร้าง Multiplexer

  * **Key Concept:** `always @(*)`, `case (sel) ... endcase`

#### **5. Sequential Logic - D Flip-Flop (`example_5`)**

หัวใจสำคัญของวงจร Digital คือวงจรความจำ (Sequential Logic) ที่ทำงานตามสัญญาณนาฬิกา (`clk`) และการใช้ **Non-blocking Assignment** (`<=`)

  * **Key Concept:** `always @(posedge clk) q <= d;`

#### **6. Parameterized Modules (`example_6`)**

การเขียน Module ให้ยืดหยุ่นและนำกลับมาใช้ซ้ำได้ (Reusable) โดยการกำหนด **Parameter** (เช่น การกำหนดความกว้างบิต `WIDTH`)

  * **Key Concept:** `module name #(parameter WIDTH = 1) ...`

-----

### 🛠️ How to Run Simulations

#### Option 1: Running Cocotb (Recommended)

Cocotb ช่วยให้เขียน Testbench ด้วยภาษา Python ซึ่งยืดหยุ่นและตรวจสอบผลลัพธ์ได้ง่าย

1.  เข้าไปที่โฟลเดอร์ `cocotb` ของ example ที่ต้องการ
    ```bash
    cd tutorials/examples/example_1/cocotb
    ```
2.  รันคำสั่ง `make` (ต้องติดตั้ง Icarus Verilog และ Cocotb ก่อน)
    ```bash
    make
    ```
3.  ผลลัพธ์ Pass/Fail จะแสดงบน Terminal ทันที

#### Option 2: Running Standard Verilog Simulation

สำหรับการดู Waveform เบื้องต้นด้วย Icarus Verilog และ GTKWave

1.  เข้าไปที่โฟลเดอร์ `sim`
2.  Compile และ Run (ตัวอย่างสำหรับ Linux/Mac):
    ```bash
    iverilog -o result.vvp sim_module.v ../src/design_module.v
    vvp result.vvp
    ```
3.  เปิดดู Waveform:
    ```bash
    gtkwave wave.vcd
    ```